---
name: qiuqiu-cover-prompt
description: "从文章、Markdown 或图片参考生成公众号/小红书封面提示词，默认 3:4 竖版，支持 10 种构图风格、真人脸与旧封面参考、长标题排版、产品/UI 素材和直接跑图。用户提到封面设计、封面提示词、公众号封面、小红书封面、跑图、根据文章生成图片或用某张脸复刻封面时使用。"
---

# 秋秋封面提示词

## 工作目标

把文章内容转成可直接粘贴到即梦、Seedream、Nano Banana、GPT Image 等模型的中文生图提示词。默认只生成提示词；用户明确要求“直接跑图”“生成封面”或“帮我出图”时，先展示最终提示词，再使用图像生成工具执行。不要把“生成提示词”和“已经生成图片”混为一谈。

## 交互流程

### 需求判断（先做，不必逐轮机械提问）

先判断用户是否已经给出了文章、标题、风格、参考图和跑图意图：

- 信息完整（例如已给文章标题、人物照片、旧封面和明确风格）时，直接提炼方案并生成提示词；不要重复询问已经回答过的问题。
- 只缺少关键决策（通常是标题是否保留、人物是否出现、或风格选择）时，只问最少的一个问题。
- 用户只想了解方案时，只输出标题候选、构图建议和提示词，不调用图像工具。
- 用户明确要直接跑图时，默认选择最匹配的风格和未指定的视觉细节，不为了凑流程阻塞生成。

### 第 1 轮：文章、风格和标题

先读取用户提供的文章、Markdown 文件或当前编辑内容。提炼文章的核心承诺、目标读者、可视化场景和 1—3 个标题候选。标题候选默认 4—12 字，但如果用户给出正式文章标题，优先逐字保留，不得擅自删改；长标题可通过换行、缩小字号、分组层级解决。仅在用户没有明确风格时推荐 1—2 种风格；需要选择时再列出 10 种风格，不要每次都重复展开。

风格的完整描述见 [references/style-guide.md](references/style-guide.md)。不要在用户尚未选择前生成最终提示词。

### 第 2 轮：参考图和主体

一次询问：

- 先给参考图分配角色，而不是按上传顺序猜用途：人物脸/半身照 = 人物身份参考；旧封面 = 风格、色彩或构图参考；产品图/UI 截图 = 主视觉素材；Logo/图标 = 品牌素材。
- 提示词中固定使用“图 1、图 2、图 3……”引用，并明确“图 1 只用于脸部身份，不照搬背景”“图 2 只用于风格/构图”等隔离规则，避免把旧封面的文字、人物或背景错误复制进新图。
- 本地图片交给图像工具前，先查看图片确认其用途；不要声称模型看到了未提供或看不清的细节。
- 若有人脸参考，使用“保持五官、发型、肤色和身份一致”，但不要承诺 100% 复刻；若用户要求口罩、大头、像素化等变形，明确这些是允许改变的部分。

### 第 3 轮：视觉细节

一次列出以下选项，用户未指定的项目交给模型决定：

- 人物表情：捂嘴惊讶、张嘴震惊、开心大笑、兴奋雀跃、自信得意、托腮思考、推荐种草感、交给模型决定。
- 背景色调：浅色系、深色系、暖色调、冷色调、高饱和撞色、交给模型决定。
- 字体风格：超粗中文黑体、柔和圆体、极简无衬线、复古宋体、短视频爆款标题风格、交给模型决定。
- 字体效果：纯白、纯黑、渐变色、描边效果、交给模型决定。

极简留白风和人物侧置留白风默认浅色背景；背影构图风跳过表情问题；产品主视觉风和局部出镜风优先询问产品素材。

若用户已在消息中指定视觉细节（例如“像素风、参考星露谷、戴口罩、大头特效”），直接采用，不再列选项。未指定的项目由文章主题和选定风格决定，并在方案摘要中标明默认值。

## 生成规范

生成最终提示词时遵守：

1. 第一行必须是“3:4 竖版构图”。
2. 标题必须用中文引号圈出，并逐字保留用户确认的文字；不要自行添加副标题、Logo、水印或其他文字。
3. 长标题优先采用 2—4 行分组排版；不要为了“更好生成”擅自改成短标题。若用户允许缩短，先给出原题与短题对照并标注取舍。
4. 所有人脸、标题、产品和关键道具距离四边至少约 10%，全部位于安全区内；为公众号裁切预留上下左右余量。
5. 具体描述人物身体位置、手部动作、主体材质与大小，以及前景、中景、后景的遮挡关系。
6. 把抽象概念转换成日常可识别的实物（例如模块卡片、工作台、书桌、清单），不使用“高级感”“做得有冲击力”替代画面描述。
7. 用正向画面描述；避免廉价霓虹光效、数据流、粒子特效、过度装饰和无意义的科技背景。
8. 参考图只写“图 1 / 图 2 / 图 3”，并说明每张图的允许用途；不要凭空复述看不清的素材细节。
9. 明确禁止额外文字、乱码、错别字、Logo、水印和模型自发添加的副标题；标题需要中文时，重复一次“逐字正确”。
10. 结尾提醒用户逐字检查中文标题；发现错字、漏字或变形时，优先重新生成或后期排版，不要接受错误文字。

## 输出格式

输出一个可复制的提示词代码块，前面简要列出：最终标题、核心卖点、构图风格、参考图安排和未指定项目的默认值。必要时在代码块后附“负面约束”小段。不要把十种模板全部展开，也不要输出与生图无关的实现细节。

如果用户要求直接跑图：先生成并展示提示词，再调用图像生成工具。使用本地参考图时，确保每张图都已查看并按角色编号；新图默认非破坏性生成，不覆盖用户原图。工具成功后展示生成结果并给出保存路径；工具失败、超时或网络错误时，明确说明“没有生成成品”，保留并交付提示词，不要虚构图片。只有用户明确同意使用 CLI/API 时，才说明需要 `OPENAI_API_KEY` 并切换到备用方式。

## 参考资料

- [style-guide.md](references/style-guide.md)：10 种构图风格的视觉规则和提示词骨架。
- [prompt-checklist.md](references/prompt-checklist.md)：生成前后的检查清单。
- [title-and-reference-rules.md](references/title-and-reference-rules.md)：长中文标题、换行策略和多张参考图的角色分配。
