---
name: qiuqiu-wechat-cover
description: >
  为「秋秋」制作微信公众号文章封面：先读取文章，提炼一个可点击的主题钩子，
  再用暖木色复古像素工作台、真人秋秋和用户提供的真实素材生成 2.35:1 横版封面。
  适用于好物分享、AI 工具测评、旅行、学习效率、数码体验和桌面改造；不适用于泛用海报或脱离正文的装饰图。
---

# 秋秋微信公众号封面

这是一套可复用的品牌封面工作流。每次只替换文章主题、封面文案和真实主体，保持固定的视觉识别。

## 工作流

按 [references/workflow.md](references/workflow.md) 执行以下阶段：

1. **收集**：先要完整文章（正文、Markdown 内容或可读取的本地路径），再确认需要的图片。
2. **分析**：从正文提取主题、点击理由、一个核心数字/结果/冲突，以及必须真实呈现的对象。
3. **提案**：给出 3 个短钩子和 1 个构图建议；用户未确认文案时不生成图片。
4. **生成或编辑**：用户明确说“生成/跑图”才调用图像工具；局部修改遵守最小变更。
5. **验收**：检查比例、中文文字、人物身份、真实素材、明度和正文一致性，报告不确定项。

文章路径不可读取、正文不完整或关键事实缺失时，暂停在当前阶段并标记「待确认」，不要凭标题或常识补写。

## 输入角色

| 输入 | 用途 | 约束 |
| --- | --- | --- |
| 默认图 1：`references/assets/qiuqiu-face-reference.jpg` | 秋秋身份 | 只参考脸、黑发、口罩、发型、肤色和年龄感；不复制背景、姿势或文字 |
| 默认图 2：`references/assets/qiuqiu-style-reference.png` | 品牌风格 | 只参考暖木像素、配色、光线、标题层级和桌面关系；不复制文案、人物、Logo 或具体产品 |
| 新增图 3+ | 产品、Logo、截图、旅行照或旧封面 | 在提示词中写明用途；有真实图就原样使用，不重绘、不换色、不换牌 |
| 文章 | 主题和事实来源 | 正文优先于标题、旧提示词和模型猜测 |

对话中附带的新参考图优先于默认图，但要先说明每张图的角色。没有真人图时不虚构秋秋；没有产品/Logo 图时使用文字名或留出干净区域。

## 不可变的品牌约束

- 画布严格 **2.35:1**，优先 1880×800；除非用户明确要其他平台版本，不改比例。
- 视觉为 **暖木色 × 复古像素游戏 × 温馨工作台 × 真实主体**，整体明亮温暖，避免暗色科技风。
- 默认构图为左侧 55%～65% 标题、右侧 30%～40% 人物或留白、下方真实主体；人物和道具不得遮挡标题、关键数字、产品或 Logo。
- 文字最多 3 组（小钩子、主标题、可选补充），总量约 20～35 个汉字；主标题是唯一视觉焦点，不塞功能清单。
- 有真人参考时保持偏真实的脸（约 80% 真人感 + 20% 像素融合），禁止换脸、娃娃脸、过度美颜和彻底像素化。
- 真实产品、Logo、包装和旅行照优先原位使用；未提供或无法确认的内容不得编造。

细化的风格参数见 [references/style-guide.md](references/style-guide.md)，可复制的跑图结构见 [references/prompt-template.md](references/prompt-template.md)。

## 新图与编辑模式

- **新封面**：先完成文章分析和文案确认，再按模板生成。
- **编辑已有封面**：只执行用户点名的变化（删一句话、换 Logo、调脸等），保留其余构图、背景、产品和文字；删除区域用周围背景自然补齐。
- **输出位置**：生成图片保存到项目目录之外。若用户没有指定目录，先询问保存位置；不要把 PNG/JPG/WebP 写入或提交到本 Skill 仓库。
- 图像工具返回失败、尺寸不符或文字不可读时，不声称成功；保留已确认的文案和提示词，说明失败原因并等待重跑或调整。

## 输出协议

每个阶段只输出当前需要的信息：

- 收集阶段：缺什么，只问什么。
- 提案阶段：`主题判断`、`3 个钩子`、`推荐构图`、`待确认素材`。
- 生成阶段：`最终文案`、`完整提示词`、`参考图角色`。
- 验收阶段：输出文件、实际尺寸/比例、通过项、待确认项和下一步修改建议。

生成前后使用 [references/prompt-checklist.md](references/prompt-checklist.md)。不要把提示词已写好说成图片已生成，也不要把模型可能写错的中文当成已验证事实。
