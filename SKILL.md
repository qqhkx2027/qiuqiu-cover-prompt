---
name: qiuqiu-wechat-cover
description: >
  为「秋秋」制作微信公众号文章封面：读取文章，按参考图角色和任务模式组织真实素材，
  生成或合成暖木色复古像素风的 2.35:1 横版封面，并在需要时用确定性文字后期排版。
  适用于好物分享、AI 工具测评、旅行、学习效率、数码体验和桌面改造；不适用于泛用海报或脱离正文的装饰图。
---

# 秋秋微信公众号封面

这是一套可复用的品牌封面工作流。每次只替换文章主题、封面文案和真实主体，保持固定的视觉识别。

## 先选模式

- **GENERATE**：从零生成背景、构图、像素装饰和可选真人。适合没有必须原样保留素材的新封面。
- **COMPOSITE**：先生成背景，再把真实产品、Logo、包装、截图或旅行照抠图/裁切/排版进去。适合“真实的、原图、保持不变、不要生造”等要求。
- **LOCAL_EDIT**：在已有封面上只修改用户点名区域。适合改字、换 Logo、修脸、删元素；不得整张重绘。

出现“真实的 / 原图 / 不要生造 / 保持不变 / 只修改 / 换成这个 Logo”时，禁止使用全画面重新生成模式。

## 角色优先的参考图

按 [references/workflow.md](references/workflow.md) 建立 `reference_manifest`。优先级是：用户明确说明的角色 ＞ 当前轮图片内容判断 ＞ 项目默认资产 ＞ 历史序号约定。图 1、图 2 只是兼容旧对话的说法，不再代表固定角色。

项目默认资产：

- `assets/qiuqiu-face-reference.jpg`：`identity`
- `assets/qiuqiu-style-reference.png`：`style`

新增图片要声明 `role`（如 `current_cover`、`identity`、`style`、`product`、`logo`、`travel_photo`、`screenshot`），并说明是否 `preserve: exact`。没有真人图时不虚构秋秋；没有真实主体图时使用文字名或留白。

视觉参数见 [references/style-guide.md](references/style-guide.md)，历史翻车与模式选择示例见 [examples/failure-cases.md](examples/failure-cases.md)。

## 工作流

按需读取参考文档，执行：

1. **收集**：先读取完整文章（正文、Markdown 内容或可读取路径），再确认模式和必要素材。
2. **分析**：提取主题、点击理由、一个核心数字/结果/冲突，以及必须真实呈现的对象。
3. **提案或直接执行**：用户说“先规划/先想标题”时给 3 个钩子并等待确认；用户说“生成封面/做一张封面/跑图”时内部选最佳钩子后直接执行；用户已给文案时原样使用。
4. **制作**：GENERATE 或 COMPOSITE 按 [references/prompt-template.md](references/prompt-template.md) 跑图；中文文字优先走 `scripts/render_cover_text.py` 后期压字。
5. **验收**：按 [references/prompt-checklist.md](references/prompt-checklist.md) 检查比例、正文一致性、文字、人物、真实素材和局部改动范围。

文章路径不可读取、正文不完整或关键事实缺失时，暂停并标记「待确认」，不要凭标题或常识补写。图像工具失败、尺寸不符或文字不可读时，不声称成功。

## 不可变的品牌约束

- 画布严格 **2.35:1**，优先 1880×800；除非用户明确要其他平台版本，不改比例。
- 视觉为 **暖木色 × 复古像素游戏 × 温馨工作台 × 真实主体**，整体明亮温暖，避免暗色科技风。
- 默认构图为左侧 55%～65% 标题、右侧 30%～40% 人物或留白、下方真实主体；不要互相遮挡。
- 文案最多 3 组（小钩子、主标题、可选补充），总量约 20～35 个汉字；主标题是唯一视觉焦点。
- 有真人参考时保持偏真实的脸（约 80% 真人感 + 20% 像素融合），禁止换脸、娃娃脸、过度美颜和彻底像素化。
- 真实产品、Logo、包装和旅行照在 COMPOSITE 中必须原样保留；未提供或无法确认的内容不得编造。

## 输出协议

- 收集：只问当前缺少的内容。
- 提案：`主题判断`、`3 个钩子`、`推荐构图`、`待确认素材`。
- 生成：`模式`、`最终文案`、`reference_manifest`、`完整提示词`、`文字后期方案`。
- 验收：外部输出文件、实际尺寸/比例、通过项、待确认项和下一步修改建议。

生成图片必须保存到项目目录之外；未指定目录时先询问。不要把 PNG/JPG/WebP 写入或提交到本 Skill 仓库。
