---
name: qiuqiu-cover-prompt
description: "从文章、Markdown 或图片参考生成秋秋同款星露谷像素风的小红书图文笔记或微信公众号头条头图提示词，支持 3:4 1080×1440px 与 2.35:1 900×383px 输出、真人脸与旧封面参考、长标题排版和直接跑图。用户提到秋秋封面、星露谷像素风、公众号封面、小红书封面、尺寸选择、跑图、根据文章生成图片或用某张脸复刻封面时使用。"
---

# 秋秋封面提示词

## 工作目标

把文章内容转成可直接粘贴到即梦、Seedream、Nano Banana、GPT Image 等模型的中文生图提示词。先让用户选择输出类型，或根据上下文采用已明确的类型：小红书图文笔记使用 3:4、1080×1440px；微信公众号头条头图使用 2.35:1、900×383px。默认只生成提示词；用户明确要求“直接跑图”“生成封面”或“帮我出图”时，先展示最终提示词，再使用图像生成工具执行。不要把“生成提示词”和“已经生成图片”混为一谈。

## 输出尺寸预设

优先使用 [references/output-presets.md](references/output-presets.md) 的预设，不要把两种画幅混用：

| 输出类型 | 画幅 | 目标尺寸 | 构图重点 |
|---|---:|---:|---|
| 小红书图文笔记 | 3:4 竖版 | 1080×1440px | 信息流占屏大，主体和标题在手机端一眼可读 |
| 微信公众号头条头图（推送列表封面） | 2.35:1 横版 | 900×383px | 横向标题区、人物/产品区分层，适合列表卡片裁切 |

如果用户没有选择类型，只问一个问题：“要生成小红书 3:4 竖图，还是微信公众号 2.35:1 横图？”如果用户说“两个都要”，分别输出两套提示词，不要用一套提示词兼容两种尺寸。

## 交互流程

### 需求判断（先做，不必逐轮机械提问）

先判断用户是否已经给出了文章、标题、参考图和跑图意图。当前版本固定使用“秋秋同款星露谷像素风”，不要求用户选择风格：

- 信息完整（例如已给文章标题、人物照片、旧封面和明确风格）时，直接提炼方案并生成提示词；不要重复询问已经回答过的问题。
- 只缺少关键决策（通常是标题是否保留或人物是否出现）时，只问最少的一个问题。
- 用户只想了解方案时，只输出标题候选、构图建议和提示词，不调用图像工具。
- 用户明确要直接跑图时，直接采用秋秋同款星露谷像素风和未指定的视觉细节，不为了凑流程阻塞生成。
- 用户明确给出平台或尺寸时，直接锁定对应预设；用户只说“封面”时，先询问平台，不要擅自默认为 3:4。

### 第 1 轮：文章和标题

先读取用户提供的文章、Markdown 文件或当前编辑内容。提炼文章的核心承诺、目标读者、可视化场景和 1—3 个标题候选。标题候选默认 4—12 字，但如果用户给出正式文章标题，优先逐字保留，不得擅自删改；长标题可通过换行、缩小字号、分组层级解决。不要询问风格，也不要列出其他风格。

固定采用 [references/style-guide.md](references/style-guide.md) 中的“秋秋同款星露谷像素风”。

### 第 2 轮：参考图和主体

一次询问：

- 先给参考图分配角色，而不是按上传顺序猜用途：人物脸/半身照 = 人物身份参考；旧封面 = 风格、色彩或构图参考；产品图/UI 截图 = 主视觉素材；Logo/图标 = 品牌素材。
- 提示词中固定使用“图 1、图 2、图 3……”引用，并明确“图 1 只用于脸部身份，不照搬背景”“图 2 只用于风格/构图”等隔离规则，避免把旧封面的文字、人物或背景错误复制进新图。
- 本地图片交给图像工具前，先查看图片确认其用途；不要声称模型看到了未提供或看不清的细节。
- 若有人脸参考，使用“保持五官、发型、肤色和身份一致”，但不要承诺 100% 复刻；若用户要求口罩、大头、像素化等变形，明确这些是允许改变的部分。

### 第 3 轮：视觉细节（仅补充，不改变主题风格）

一次列出以下选项，用户未指定的项目交给模型决定：

- 人物表情：捂嘴惊讶、张嘴震惊、开心大笑、兴奋雀跃、自信得意、托腮思考、推荐种草感、交给模型决定。
- 背景色调：浅色系、深色系、暖色调、冷色调、高饱和撞色、交给模型决定。
- 字体风格：默认粗体像素中文字体；只有用户明确要求时才改字体。
- 字体效果：纯白、纯黑、渐变色、描边效果、交给模型决定。

若用户已在消息中指定视觉细节（例如“像素风、参考星露谷、戴口罩、大头特效”），直接采用，不再列选项。未指定的项目由文章主题和固定风格决定，并在方案摘要中标明默认值。

## 生成规范

生成最终提示词时遵守：

1. 第一行必须写已选预设的画幅和尺寸：小红书为“3:4 竖版构图，输出尺寸 1080×1440px”；微信公众号为“2.35:1 横版构图，输出尺寸 900×383px”。
2. 标题必须用中文引号圈出，并逐字保留用户确认的文字；不要自行添加副标题、Logo、水印或其他文字。
3. 长标题优先采用 2—4 行分组排版；不要为了“更好生成”擅自改成短标题。若用户允许缩短，先给出原题与短题对照并标注取舍。
4. 所有人脸、标题、产品和关键道具距离四边至少约 10%，全部位于安全区内；小红书重点适配手机信息流，公众号头图重点适配横向列表卡片裁切。
5. 具体描述人物身体位置、手部动作、主体材质与大小，以及前景、中景、后景的遮挡关系。
6. 把抽象概念转换成日常可识别的实物（例如模块卡片、工作台、书桌、清单），不使用“高级感”“做得有冲击力”替代画面描述。
7. 用正向画面描述；避免廉价霓虹光效、数据流、粒子特效、过度装饰和无意义的科技背景。
8. 参考图只写“图 1 / 图 2 / 图 3”，并说明每张图的允许用途；不要凭空复述看不清的素材细节。
9. 明确禁止额外文字、乱码、错别字、Logo、水印和模型自发添加的副标题；标题需要中文时，重复一次“逐字正确”。
10. 结尾提醒用户逐字检查中文标题；发现错字、漏字或变形时，优先重新生成或后期排版，不要接受错误文字。

## 输出格式

输出一个可复制的提示词代码块，前面简要列出：输出类型、画幅/尺寸、最终标题、核心卖点、固定风格、参考图安排和未指定项目的默认值。必要时在代码块后附“负面约束”小段。不要输出其他风格选项或与生图无关的实现细节。

如果用户要求直接跑图：先生成并展示提示词，再调用图像生成工具。使用本地参考图时，确保每张图都已查看并按角色编号；新图默认非破坏性生成，不覆盖用户原图。工具成功后展示生成结果并给出保存路径；工具失败、超时或网络错误时，明确说明“没有生成成品”，保留并交付提示词，不要虚构图片。只有用户明确同意使用 CLI/API 时，才说明需要 `OPENAI_API_KEY` 并切换到备用方式。

## 参考资料

- [style-guide.md](references/style-guide.md)：秋秋同款星露谷像素风的视觉规则和两种画幅骨架。
- [prompt-checklist.md](references/prompt-checklist.md)：生成前后的检查清单。
- [title-and-reference-rules.md](references/title-and-reference-rules.md)：长中文标题、换行策略和多张参考图的角色分配。
- [output-presets.md](references/output-presets.md)：小红书与微信公众号两种尺寸预设及构图注意事项。
