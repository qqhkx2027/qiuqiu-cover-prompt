# 工作流与输出协议

这份文档只处理“什么时候做什么”。视觉参数读 [style-guide.md](style-guide.md)，跑图时读 [prompt-template.md](prompt-template.md)，验收清单读 [prompt-checklist.md](prompt-checklist.md)。

## 输入清单

| 项目 | 来源 | 必需性 | 处理方式 |
| --- | --- | --- | --- |
| 完整文章 | 用户 | 必需 | 粘贴正文、Markdown 或可读取路径；只有标题时标记「待确认」 |
| 身份图（图 1） | Skill 内置 | 默认必需 | 自动加载 `references/assets/qiuqiu-face-reference.jpg`，不要向用户重复索取 |
| 风格图（图 2） | Skill 内置 | 默认必需 | 自动加载 `references/assets/qiuqiu-style-reference.png` |
| 真实主体图 | 用户按需 | 按当前文章需要 | 产品、Logo、截图、旅行照等；只索取封面确实需要的图 |
| 新真人参考图 | 用户（明确指定时） | 可选 | 只有用户明确“这张图作为秋秋真人参考”才覆盖 图 1 |
| 旧封面 | 用户 | 仅编辑模式 | 用于最小范围修改；先确认要改的对象和保留项 |

默认角色：**图 1 = Skill 内置身份图，图 2 = Skill 内置风格图，图 3+ = 当前任务特有素材。**

用户上传的普通产品图、旅行图、截图**不会自动覆盖**默认身份或风格参考；除非用户明确指定角色，否则一律从图 3+ 开始编号。输出时要写清楚“这张图用于什么”，不要把无角色的图混成一个参考图列表。

## 内置资产加载（生成前强制）

调用图像工具前，先运行：

```bash
python3 tools/resolve_assets.py
```

结果必须是 `ok: true` 且两个资产都有有效 `absolute_path`。如果当前运行时不能直接把本地路径作为图片输入，用：

```bash
python3 tools/resolve_assets.py --data-uri
```

拿到 data URI 后，仍要以当前图像工具真正支持的图片输入方式把 图 1 / 图 2 传进去。**在提示词里写文件名或“参考图 1”不代表图像模型已经看到图。**

任何一步无法真实传入：

1. 依次尝试相对路径、绝对路径、file 引用、bytes、data URI。
2. 仍然失败则停止生成，报告“当前运行时不支持把 Skill 内置图片传给图像工具”，标记「待确认」。
3. 不虚构“像秋秋”的人物，不搜索外部替代图，不要求用户重复上传内置资产。

## 五个阶段

### 1. 收集

读取文章并确认素材。一次只追问当前阶段缺少的内容：缺文章只问文章，缺必要图片只问图片。

### 2. 分析

从正文记录：

- 一句话主题
- 读者点击理由
- 一个核心数字、结果或冲突
- 必须真实呈现的对象
- 可以从封面删掉的信息

每项都要能在正文中找到依据；找不到就写「待确认」。

### 3. 提案

输出固定结构：

```text
主题判断：...
钩子 A：...
钩子 B：...
钩子 C：...
推荐构图：...
待确认素材：...
```

优先使用结果型、问题型或反差型钩子。用户未确认文案前，不使用图像工具。

### 4. 生成或编辑

用户明确“生成/跑图”后：

1. 运行 `tools/resolve_assets.py`，确认图 1（身份）与图 2（风格）已真正可读。
2. 按当前图像工具支持的方式注入 图 1 / 图 2，把当前任务素材（如有）编号为 图 3+。
3. 用 [prompt-template.md](prompt-template.md) 组装提示词，写明每张图的角色、准确中文文案、布局和“不添加未授权文字”。
4. 生成文件保存到项目目录之外；未指定保存目录时先询问。

用户要求局部修改时，只改点名区域，其他区域一像素不动（编辑政策见 [prompt-checklist.md](prompt-checklist.md) 生成后部分与 [SKILL.md](../SKILL.md) 的新图与编辑模式）。

### 5. 验收

至少检查：实际宽高是否 2.35:1、正文主题、中文逐字、真人身份、真实素材、Logo、明度、遮挡和未授权内容。结果不确定就写「待确认」，不要把模型输出当事实。

## 停止条件与恢复

- **缺正文或路径不可读**：停止分析，要求文章。
- **内置资产无法加载或注入**：按上文尝试失败后停止，说明是运行时能力限制。
- **缺必要真实素材**：说明缺哪张以及可用的文字名/留白替代，等待用户选择。
- **文案未确认**：停在提案阶段。
- **生成失败或验收不通过**：保留已确认文案和提示词，只调整失败项；下一轮仅重跑失败部分。

## 建议的最终回报

```text
已生成：/absolute/path/outside/project/cover.png
尺寸：1880×800（2.35:1）
文案：…
参考图：图 1=身份(内置图已真实传入)，图 2=风格(内置图已真实传入)，图 3+=真实主体
已检查：…
待确认：…
```

